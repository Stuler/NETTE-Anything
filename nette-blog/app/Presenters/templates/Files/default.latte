{block content}

	<link rel="stylesheet" href="{$basePath}/css/style.css">
	<link rel="stylesheet" href="{$basePath}/css/normalize.css">
	<link rel="stylesheet" href="{$basePath}/css/bootstrap.css">

	<main>
    <div>
        <p><a n:href="Homepage:default">Zpět na homepage</a></p>
    </div>

    <div>
        {control formUpload}
    </div>

    <div>
        {control formCreate}
    </div>

    <div>
        <h3>Strom</h3>
        {*TODO pridat zobrazenie suborov a zloziek*}
        {*
        pre zvyraznenie zlozky si porovnam id v URL s id zlozky -> pripisem styl
        *}

        <ul>
            {foreach $items as $item}
                <li>
                    {if $item[is_dir]}
                        <a n:class="$selectedId == $item[id] ? selected" n:href="default id=>$item[id],level=>$item[level],is_dir=>$item[is_dir]">{$item[name]}</a>
                        <ul>
                            {foreach $item[items] as $subItem}
                                <li>
                                    {if $subItem[is_dir]}
                                        <a n:class="$selectedId == $subItem[id] ? selected" n:href="default id=>$subItem[id],level=>$subItem[level], is_dir=>$subItem[is_dir]">{$subItem[name]}</a>
                                    {else}
                                        {$subItem[name]}
                                    {/if}
                                </li>
                            {/foreach}
                        </ul>
                    {else}
                        {$item[name]}
                    {/if}
                </li>
            {/foreach}
        </ul>
    </div>
	</main>

	{* Riesenie rekurziou:
		- zistim level elementu
		- ak ma level 1, necham tak
		- ak nema level 1, zistim rodica
		- zistujem level rodicov, kym nedojdem na level1
	*}

    <script src="https://nette.github.io/resources/js/3/netteForms.min.js"></script>
{/block}